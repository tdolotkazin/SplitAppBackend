MONGODB_URI=
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_HOSTS=
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_AUTH_SOURCE=splitapp
MONGODB_REPLICA_SET=
MONGODB_TLS=false
MONGODB_TLS_CA_FILE=
MONGODB_DB_NAME=splitapp
JWT_SECRET=
JWT_ACCESS_EXPIRE_MINUTES=15
JWT_REFRESH_EXPIRE_DAYS=30
# Optional legacy static bearer (same value as old AUTH_TOKEN). Skips JWT and per-user membership checks.
AUTH_TOKEN=

# Yandex Object Storage (S3-compatible). When both are set, passed explicitly to session.client().
S3_ENDPOINT_URL=https://storage.yandexcloud.net
S3_REGION=ru-central1
YC_OBJECT_STORAGE_ACCESS_KEY_ID=
YC_OBJECT_STORAGE_SECRET_ACCESS_KEY=
S3_BUCKET=
